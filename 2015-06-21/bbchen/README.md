## 轮播图

### 一：涵盖知识

#### （1）DOM操作

#### （2）定时器

【1】setinterval（）
    按照指定周期（毫秒）来调用函数或计算表达式
    不停调用该函数，直至clearInterval()被调用或关闭窗口，由该函数返回的ID值可用作clearInterval()方法的参数

【2】setTimeout()
   setTimeout() 方法用于在指定的毫秒数后调用函数或计算表达式。只执行一次。

#### （3）事件运用

#### （4）JS动画

#### （5）递归

#### （6）无限滚动

### 二：实现

#### 1：HTML

#### 2：CSS

#### 3：JS实现

##### (1) 箭头切换:

【1】获取left值list.style.left,用parseInt去单位
【2】因为左右都是用通过运算来获得新的left的值，所以可以写一个函数封装起来，只要调用这个函数即可
【3】定义这个函数animate

##### (2)无限滚动

【1】要用两张图用来做切换时的过度
【2】判断图比第一张图的位置大，或者比最后一张图小，如果成立，直接切换left的位置进行归为

##### (3)亮小圆点

【1】要让小圆点亮起来，showbutton（），用classNmae = “”，来修改属性值，控制小圆点颜色
【2】亮起来圆点，也要置灰亮点
【3】继续切换时还要调index的值

##### (4)按钮切换

【1】给每个元素加上事件，用for循环编译buttons数组
【2】是随机切换按钮的，所以要算出他的偏移量
【3】取index的值确定当前是第几张图片[不能this.index(不能获取自定义属性的值），getAttribute（””）可以获得属性值
【4】还要调用showbutton亮圆点

##### （5）动画函数

【1】需要定义变量，位移总时间time， 位移间隔时间interval，速度speed = 总偏移量 / （位移总时间 / 唯一间隔）---每一次的位移量 
【2】增加一个go函数，判断什么时候做位移，两个条件speed&lt;0--左移&&偏移量&gt;前一个偏移量 ||speed&gt;0--右一&&偏移量&lt;上一个偏移量,更新偏移量，如果不满足就是到了边界，就执行（2）中的偏移量的更新
【3】定时器settimeout（函数，时间）
【4】加一个判断动画是否执行的函数animated

##### （6）自动切换

【1】定义一个函数play（）,和一个stop（）函数；
【2】用到另一个可多次执行的定时器

### 三：last but not least

听不懂没关系，因为我还有终极武器。

（往下拉）

慕课网，你学习的好伙伴（我没有收广告费）   
[example link](http://www.imooc.com/learn/18)   
毕竟渣渣，我是看了三次视频，敲了三次代码才理解到一点点皮毛的，所以讲得不清不楚，不伦不类。大家那么厉害，祝一遍就掌握。干吧爹~~~